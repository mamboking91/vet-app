/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // Por ejemplo, si quieres asegurarte de que reactStrictMode esté activado (aunque suele serlo por defecto):
  // reactStrictMode: true,
};

module.exports = nextConfig;